<% if current_user != user %>
  <% if current_user.is_following?(user)%>
    <%= link_to "Unfollow", followship_path(current_user) %>
  <% else %>
    <%= link_to "Follow", followships_path(current_user) %>
  <% end%>
<% end%>