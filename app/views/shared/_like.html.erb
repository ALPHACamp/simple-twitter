<% if current_user.like?(tweet) %>
  <%= link_to "Unlike (#{tweet.likes_count})", unlike_tweet_path(tweet), method: :post, class: "btn btn-link" %>
<% else %>
  <%= link_to "Like (#{tweet.likes_count})", like_tweet_path(tweet), method: :post, class: "btn btn-default"  %>
<% end %>
