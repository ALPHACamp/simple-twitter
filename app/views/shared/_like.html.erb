<% if tweet.is_liked?(current_user) %>
  <%= link_to "unlike", unlike_tweet_path(tweet), method: :post, class: "links links-red" %>
<% else %>
  <%= link_to "likes(#{tweet.likes_count})", like_tweet_path(tweet), method: :post, class: "links links-red" %>
<% end %>