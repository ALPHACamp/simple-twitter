<div class="col-md-2">

  <div class="profile_name">
    <%= render partial: "shared/avatar", locals: {avatar: user.avatar} if user.avatar %>
  </div>

  <div class="profile_name"><%= user.name %></div>
  <div class="intro"><%= user.introduction %></div>

  <!-- user link -->
  <div class="link">
    <%= link_to "Tweets : #{user.tweets.count}", users_tweets_path(user) %><br/>

    <%= link_to "Following : #{user.followings.count}", following_user_path(user) %><br/>

    <%= link_to "Follower : #{user.followers.count}", user_follower_path(user) %><br/>

    <%= link_to "Like : #{user.liked_tweets.count}", user_likes_path(user) %>
  </div>

  <div class="button-group">
    <%= render partial: "shared/edit", locals: {user: user} %>
  </div>

</div>