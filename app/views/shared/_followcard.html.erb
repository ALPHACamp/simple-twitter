<% users.each do |user| %>
  <div class="col-sm-6 follorcard" >
    <div class="thumbnail">
        <%= render partial: "shared/pic", locals: {user: user} %>
      <div class="caption">
        <h3><%= link_to "#{user.name}", tweets_user_path(user) %></h3>
        <p><%= user.introduction %></p>
        <%= render partial: "shared/follow", locals: {user: user} %>
      </div>
    </div>
  </div>
<% end %>  