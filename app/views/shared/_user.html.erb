<div class="col-md-12">
  <div class="tweets-items row">
    <div class="col-md-4">
      <%= image_tag user.avatar, class: "avatar-styled" if user.avatar? %>
    </div>
    <div class="col-md-8">
      <h3><%= link_to "@#{user.name}", tweets_user_path(user), class: "tweet-title" %></h3>
      <p><%= user.introduction %></p>
      <% if user != current_user %>
        <div class="btn-right">
          <% if current_user.following?(user)%>
            <%= link_to 'Unfollow', followship_path(user), method: :delete, class: "btn btn-primary" %>
          <% else %>
            <%= link_to 'Follow', followships_path(following_id: user), method: :post, class: "btn btn-primary" %>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>