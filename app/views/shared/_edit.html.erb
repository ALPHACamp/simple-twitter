<% if @user != current_user %>
  <!-- Follow / Unfollow -->
  <%= render partial: "shared/follow", locals: {user: @user} %>
<% else %>
  <!-- Edit Profile -->
  <%= link_to 'Edit Profile', { :controller => 'users', :action => 'edit'}, class: "btn btn-default" %>
<% end %>