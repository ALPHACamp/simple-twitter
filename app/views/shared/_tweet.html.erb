<% @tweets.each do |tweet| %>
        <div class="row">
          <div class="col-md-3">
            <%= image_tag tweet.user.avatar, size:50, class: "img-responsive" %>
          </div>
          <div class="col-md-9">
            <h4><%= tweet.user.name %>,<%= tweet.created_at %></h4>
            <p><%= simple_format tweet.description %></p>
          </div>
            
        </div>
          
        <hr>
        <% end %>