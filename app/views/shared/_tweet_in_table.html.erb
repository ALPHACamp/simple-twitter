<table class="table my-tweet">
  <tbody>
    <tr>
      <th scope="row">
        <% if tweet.user.avatar? %>
          <%= image_tag tweet.user.avatar, width: '100px', class: "rounded m-auto d-block" %>
        <% else %>
          <p class="text-center">No User Image</p>
        <% end %>
      </th>
      <td>      
        <%= link_to tweets_user_path(tweet.user) do %>
          <h4 class="card-text">@<%= tweet.user.name %><% end %>
          , <%= tweet.created_at.strftime("%Y-%m-%d, %H:%M") %>
          </h4>
        
        <p class="card-text"><%= tweet.description %></p>
        <!-- replay link -->
        <%= link_to tweet_replies_path(tweet), class: "btn btn-link" do %>
          Reply <span class="badge badge-pill badge-primary"><%= tweet.replies_count %></span>
        <% end %>
       
        <!-- like action button -->
        <%= render partial: "shared/like", locals: {tweet: tweet} %>
      </td>
    </tr>
  </tbody> 
</table>  
