<% if current_user&.follow?(user.id) %>
  <%= link_to "Unfollow", followship_path(user), method: :delete, remote: true %>
<% else %>
  <%= link_to "Follow", followships_path(id:user), method: :post, remote: true %>
<% end %>