<!DOCTYPE html>
<html>
  <head>
    <title>SimpleTwitter</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <!-- 導覽列 -->
    <nav class="navbar navbar-default">
      <div class="container-fluid" id="nav">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to image_tag('logo.png'), root_path, :alt => "HomePage", class: "logo" %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="pull-right text-right nav navbar-nav">
            <% if current_user %>
              <% if current_user&.admin? %>
                <li>
                  <%= link_to image_tag('admin-logo'), admin_tweets_path, :alt => "AdminPanel", class: "admin-logo" %>
                </li>
              <% end %>
            <li>
              <%= link_to image_tag(current_user.avatar), tweets_user_path(current_user), :alt => "Edit youself!", class: "head-shot" %>
            </li>
            <li><%= link_to(image_tag('logout.png'), destroy_user_session_path, method: :delete, class: "log-out") %></li>
            <!-- 暫時移除編輯個人資料連結 id = 1 -->
            <!-- 移除修改個人密碼的連結 id = 2 -->
            <% else %>
            <li><%= link_to image_tag('sign_up.png'), new_user_registration_path, :alt => "RegistrationPage", class: "logo" %></li>
            <li><%= link_to image_tag('sign_in.png'), new_user_session_path, :alt => "SignInPage", class: "logo" %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    
    <% if flash[:notice] %>
    <div class="container">
      <div class="row">
        <div class="alert alert-success alert-dismissible" role="alert">
          <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <%= notice %>
        </div>
      </div>
    </div>
    <% end %>

    <% if flash[:alert] %>
    <div class="container">
      <div class="row">
        <div class="alert alert-danger alert-dismissible" role="alert">
          <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <%= alert %>
        </div>
      </div>
    </div>
    <% end %>
    
    <%= yield %>
  </body>
</html>
