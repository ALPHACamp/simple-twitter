<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "sample Twitter", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <% if current_user&.admin? %>
            <li><%= link_to 'Admin Panel', admin_restaurants_path %></li>
          <% end %>

          <li>
            <%= link_to tweets_user_path(current_user), class: "navbar-brand" do %>
              <%= image_tag current_user.avatar, size: "40", style: "position:relative;top:-10px" %>
            <% end %>
          </li>
          <li class="dropdown">
            <a href="#" class="dorpdown-toggle" data-toggle="dropdown" aria-expanded="false">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to('登出', destroy_user_session_path, method: :delete) %></li>
              <li class="divider"></li>
              <li><%= link_to('修改個人資料', edit_user_path(current_user)) %></li>
              <li><%= link_to('修改密碼', edit_user_registration_path) %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to('註冊', new_user_registration_path) %></li>
          <li><%= link_to('登入', new_user_session_path) %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
