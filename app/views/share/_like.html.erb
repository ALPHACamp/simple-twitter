<% if tweet.is_liked?(current_user) %>
  <%= link_to "UnLike", unlike_tweet_path(tweet.id), class: "my-btn btn btn-xs btn-primary", method: :post %>
<% else %>
  <%= link_to "Like", like_tweet_path(tweet.id), class: "my-btn btn btn-xs btn-primary", method: :post %>
<% end %>