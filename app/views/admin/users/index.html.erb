<% @users.each do |user| %>
	<%= user.name %>
	<%= "Tweets number: #{user.tweets.size} "%>
	<%= "Followigs number: #{user.followings.size} "%>
	<%= "Followers number: #{user.followers.size} "%>
	<%= "Followers count: #{user.followers_count} "%>
	<%= "Likes count: #{user.likes_count} "%>
	<br>
<% end %>