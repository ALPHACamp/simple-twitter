var id = '#reply-<%= @reply.id %>'
var html = '<%= j render partial: 'share/comment', locals: { reply: @reply } %>'
$('#tweet-wall').children(id).find('.edit-tweet-form').css('display', 'none');
$('#tweet-wall').children(id).find('.show-area').css('display', 'grid');
console.log($('#tweet-wall').children(id).find('p'));
$('#tweet-wall').children(id).find('p').html(html);