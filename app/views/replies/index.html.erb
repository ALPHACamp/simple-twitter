<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-4">
    <!-- user's info -->
      <%= render partial: "shared/user_info", locals: {user: @tweet.user} %>
    </div>  
    <div class="col-12 col-sm-8">
    <!-- tweet -->
    
    <%= render partial: "shared/tweet_in_table", locals: {tweet: @tweet} %>  
    
    <!-- tweet.replies show -->
    <% @replies.each do |reply| %>
      <%= render partial: "shared/reply_in_table", locals: {reply: reply} %>  
    <% end %> 
    <!-- tweet.replies create form -->
    <%= form_for [@tweet, @reply] do |f| %>
      <div class="form_group">
        <%= f.text_area :comment, placeholder: "Reply this tweet", class: "form-control", rows: "3" %>
      </div>
      <div class="form-group text-right">
        <%= f.button "Reply",type: :submit, class: "btn btn-primary mt-1" %>
      </div>
    <% end %>
    </div>
  </div>
</div>
