<%= render partial: "shared/userprofile" %>
<div class="tweet-detial">
	<% @tweets.each do |tweet|  %>
		<%= render partial: "shared/tweet", locals: {tweet: tweet} %>
	<% end %>
	<div class="kar"><%= paginate @tweets %></div>
</div>