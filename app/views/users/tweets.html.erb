
<div >
  <div >
    <%= render partial: "shared/profile", locals: { user: @user} %>
  </div>
  <div class>
    <div class>
      <% @tweets.each do |tweet| %>
        <%= render partial: "shared/message", locals: {message: tweet, message_content: tweet.description, message_btn: true} %>
      <% end %>
    </div>
  </div>
</div>
<%= render partial: "shared/backhome" %>
