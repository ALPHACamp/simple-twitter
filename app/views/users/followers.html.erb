<div class="container">

  <!-- user info panel -->
  <div class="col-sm-3">
    <%=render partial: "/common/dashboard", locals:{user: @user}%>
  </div>


<!-- user's tweets -->
  <div class="col-sm-9">
    <!-- userlist wall-->
    <h2>Follower</h2>
    <!---  測試是否有照追蹤時間排序  
    <% if false %>
     <% @followers.each do |follower|%>
      <p>
        name: <%=follower.name%> <br>
        follwing date: <%= follower.following_date(@user) %>
      </p>
      <hr>
    <%end%>
    <% end %> 
  -->
    <%= render partial: "common/userlistwall", locals:{users: @followers} %>
  </div>

</div>