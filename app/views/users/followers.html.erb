<%= render partial: "shared/userprofile", locals: {user: @user} %>
<div class="followship-detial">
  <h1>Following</h1>
	<% @followers.each do |follower| %>
		<%= render partial: "shared/followship", locals: {following: follower} %>
	<% end %>
	<div class="kar"><%= paginate @followers %></div>
</div>