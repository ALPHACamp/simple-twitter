<div class="show-user-profile-page">
  <div class="user-profile">
    <%= render partial: "shared/profile", locals: { user: @user } %>
  </div>

  <div>
    <h3>Like</h3>
    <% @liked_tweets.each do |liked_tweet| %>
      <div class="tweet-box">
        <%= render partial: "shared/tweets", locals: { tweet: liked_tweet } %>
      </div>
    <% end %>
  </div>
</div>
