<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render partial: "shared/info", locals: { user: @user} %>
    </div>
    <div class="col-md-9">
      <% @likes.each do |l| %>
        <%= render partial: "shared/tweet_msg", locals: { tweet: l} %>
      <% end %>
    </div>
  </div>
</div>
