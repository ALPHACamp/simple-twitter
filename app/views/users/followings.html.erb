<div class="container">
  <%= render partial: "shared/info", locals: {user: @user} %>
  <div class="row">
    <div class="follow col-sm-8"">
      <%= @followings.each do |following| %>
        <%= render partial: "shared/followship", locals: {user: following} %>
      <% end %>
    </div>
  </div>
</div>   